# Language-family-trees dependencies (Python 3.10+)
# Core — needed for the notebook (LanguageTrees.ipynb) and the headline scripts:
numpy>=1.24
scipy>=1.10
matplotlib>=3.7
dendropy>=4.6
uroman>=1.3

# Method/research arms (optional — each arm script needs its own; the notebook shows
# committed figures so it runs on the core deps alone):
lingpy>=2.6          # cognate_arm.py (LexStat), asjp_tree.py
epitran>=1.25        # bible_ipa.py (grapheme->IPA)
panphon>=0.20        # epitran dependency
pyglottolog>=3.14    # langtree.gold_newick_from_glottolog (needs a glottolog/ checkout)
fasttext>=0.9.2      # corpus_check.py (GlotLID language ID)
splitspy>=0.0.4      # reticulation.py (NeighborNet)
datasets>=2.0        # (only if pulling FLORES via HF; we use the direct tarball instead)
huggingface_hub>=0.20
